doctype html
html
    head
        title Centros Deportivos
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css")
        style.
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }

            body {
                font-family: Arial, sans-serif;
                background-color: #f4f4f4;
                overflow: hidden; /* Elimina el scroll de la página */
            }

            .container {
                max-width: 1400px;
                margin: 0 auto;
                padding: 20px;
            }

            .title {
                background-color: #4CAF50;
                color: white;
                text-align: center;
                padding: 10px;
                border-radius: 10px;
                margin-bottom: 20px;
            }

            .grid {
                display: grid;
                grid-template-columns: repeat(2, 1fr); /* Dos columnas */
                gap: 20px;
                height: 85vh; /* Limitar la altura total para evitar el scroll */
            }

            .card {
                background-color: white;
                border-radius: 10px;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
                overflow: hidden;
                text-align: center;
                display: flex;
                flex-direction: column;
                /*justify-content: space-between;*/
                height: 100%; /* Asegura que todas las tarjetas ocupen el mismo espacio */
            }

            .card img {
                width: 100%;
                height: 70%; /* Ajusta la altura de la imagen para evitar que ocupe demasiado espacio */
                object-fit: cover;
            }

            .card button {
                background-color: transparent;
                border: 2px solid #4CAF50;
                border-radius: 20px;
                padding: 10px 20px;
                margin: auto auto;
                cursor: pointer;
                font-size: 16px;
                color: #4CAF50;
                transition: 0.3s;
                width: 50%;
            }

            .card button:hover {
                background-color: #4CAF50;
                color: white;
            }

    body
        .container
            .title
                h1 Centros Deportivos Disponibles

            .grid
                .card
                    img(src="img/center_1.png")
                    button Mas Información
                .card
                    img(src="img/center_2.png" alt="Centro Deportivo 2")
                    button Mas Información
                .card
                    img(src="img/center_3.png" alt="Centro Deportivo 3")
                    button Mas Información
                .card
                    img(src="img/center_4.png" alt="Centro Deportivo 4")
                    button Mas Información
